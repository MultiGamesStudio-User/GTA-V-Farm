mss>=9.0.2
opencv-python>=4.10.0
numpy>=1.26.0
pyautogui>=0.9.54
pydirectinput>=1.0.4
pywin32>=306
psutil>=5.9.0
Pillow>=10.0.0
pytesseract>=0.3.10
keyboard>=0.13.5
pynput>=1.7.6

# Auto Bouffe IA locale (vision-language) — lourd (~2 Go modèle inclus
# téléchargement HuggingFace au premier lancement), installer seulement si
# cette fonctionnalité est utilisée. Tourne sur CPU par défaut ; utilise le
# GPU (CUDA) automatiquement si présent — installer torch avec le bon index
# --index-url https://download.pytorch.org/whl/cuXXX (voir pytorch.org) pour
# profiter du GPU, sinon le wheel par défaut/CPU convient aussi.
torch>=2.2.0
torchvision>=0.17.0
# moondream2's custom modeling code (trust_remote_code) predates transformers'
# v4.50 GenerationMixin/PreTrainedModel split and v5's config internals rewrite
# — pin below both or model loading raises AttributeError on PhiConfig.
transformers>=4.46.0,<4.50.0
einops>=0.7.0
