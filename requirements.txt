mss>=9.0.2
opencv-python>=4.10.0
numpy>=1.26.0
pyautogui>=0.9.54
pydirectinput>=1.0.4
pywin32>=306
psutil>=5.9.0
Pillow>=10.0.0
pytesseract>=0.3.10
keyboard>=0.13.5
pynput>=1.7.6

# Auto Bouffe IA locale (torch/transformers/einops, ~2-4 Go avec le modèle) —
# volontairement PAS dans ce fichier : prepare-python.js l'utilise pour
# préparer python-embed/ au moment du build, et ces paquets bundlés dans
# chaque installeur/portable alourdiraient le téléchargement pour tout le
# monde, même ceux qui n'utilisent jamais cette fonctionnalité. Ils sont
# installés automatiquement à la place, au premier lancement réel de l'app
# (voir electron-app/main.js → ensureAiDeps(), et requirements-ai.txt).
